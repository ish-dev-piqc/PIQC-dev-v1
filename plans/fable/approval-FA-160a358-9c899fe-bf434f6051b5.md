---
run_id: FA-160a358-9c899fe-bf434f6051b5
decision: approve-with-upgrades
base: 160a358
head: 9c899fe
digest: bf434f6051b5
skill_rev: 195bd32
date: 2026-07-08
models: haiku(triage) / sonnet(review) / sonnet(verify) / fable(adjudicate)
---

# Fable Audit approval record — FA-160a358-9c899fe-bf434f6051b5

**Decision: Approve with upgrades.** Protocol Awareness Layer v1 (branch `fable/protocol-awareness-layer`, head `9c899fe`). 3 candidates → 3 confirmed · 0 refuted · 0 needs-human. Tier T3, 0 unresolved downstream edges — the confirmed High (M1) is not on an unresolved edge, so the Block branch does not apply.

## Confirmed findings

### M1 — macro · high · high — "awaiting review" vocabulary collision
- **Where:** `supabase/migrations/20260722000000_protocol_notices.sql:353-355` (notice detail text) vs `src/components/sotr/WorksheetItemsList.tsx:87` (drawer chip) via `src/lib/sotr/sourceEvidenceApi.ts:126-129`.
- **Problem:** the `low_confidence_extraction` notice's detail says items are "low-confidence or awaiting review", but its predicate is `confidence_state IN ('low','needs_review')` (migration line 311) — a different column than the drawer's own "N awaiting review" chip, whose documented single-source-of-truth predicate is `!review_status || review_status === 'draft'`. Both render in the same drawer, same protocol scope, with no gating: two different numbers under one phrase.
- **Smallest safe fix:** new append-only migration rewording the notice detail to confidence vocabulary only (e.g. "flagged low-confidence — verify against the source before relying on them"), dropping the words "awaiting review".
- **Owner:** @fable-dev-piqc · effort xs · allowed_paths: `supabase/migrations/*` (new migration) · Approved-by: @rv61.

### M2 — macro · medium · medium — NoticeRail fires RPCs in pick-mode
- **Where:** `src/components/sotr/SourceTruthListDrawer.tsx:93` mounts `<NoticeRail>` unconditionally; pick-mode is a real reachable path (`RiskTaggingForm.tsx:422-429` passes `onPick`); `NoticeRail.tsx:75-96` fires sync→fetch unconditionally on mount.
- **Problem:** in the auditor's attach-a-source pick flow the rail still runs its sync→fetch RPC pair (and can render notices) even though the task is item selection, not protocol review.
- **Smallest safe fix:** gate the mount — `{!onPick && <NoticeRail protocolId={studyId} />}`.
- **Owner:** @ish-dev-piqc · effort xs · allowed_paths: `src/components/sotr/SourceTruthListDrawer.tsx`.

### D1 — design-ux · medium · medium — late-resolving rail above content causes layout shift, deviates from precedent
- **Where:** `src/components/sotr/SourceTruthListDrawer.tsx:90-94`; `NoticeRail.tsx:117` (renders null until its sequential sync→fetch chain resolves); `WorksheetItemsList.tsx:40-59,103-130` (immediate placeholder, single faster fetch).
- **Problem:** notices pop in late and push down a list the user has already started reading. The cited "silent-with-signal" precedent (`ActionCardRail`) mounts **below** primary content in both existing usages (`CraWorkspaceShell.tsx:211-223`, `ProtocolIntelligenceTab.tsx:194-206`) — the above-content placement is a genuine deviation.
- **Smallest safe fix:** move `<NoticeRail>` below `<WorksheetItemsList>` (matches precedent; composes with the M2 gate into one edit).
- **Owner:** @fable-dev-piqc · effort xs · allowed_paths: `src/components/sotr/SourceTruthListDrawer.tsx` · Approved-by: @ish-dev-piqc (already listed in `plans/fable/protocol-awareness-layer.md`).

## Apply set

- **Batch 1 (@ish-dev-piqc + @fable-dev-piqc, one edit):** M2 + D1 — same file, same lines; `{!onPick && <NoticeRail .../>}` placed after `WorksheetItemsList`. Paths: `src/components/sotr/SourceTruthListDrawer.tsx`.
- **Batch 2 (@fable-dev-piqc):** M1 — new append-only migration rewording the `low_confidence_extraction` detail template. Paths: `supabase/migrations/`. Approved-by: @rv61.

## Non-findings (checked, no defect)

- Micro migration correctness (predicates, idempotent sync, RLS) — clean.
- Micro adapter + api correctness (`actionsApi` / notices adapter, Result<T>, race guards) — clean.
- Micro mount architecture (non-mode `src/components/actions/` placement, no cross-mode imports) — clean.

## Telemetry

candidates 3 → confirmed 3 · refuted 0 · needs-human 0 · CI-duplicates suppressed 0 · verify agents 3 (blind, disconfirm-first).

**Handoff:** `/fable-apply FA-160a358-9c899fe-bf434f6051b5 <finding-ids>`
