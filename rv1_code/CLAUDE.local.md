# Local overrides — do not commit (add CLAUDE.local.md to .gitignore)

## My environment
- Node version:
- Package manager: (npm / pnpm / yarn)
- Local DB: (postgres port, connection string, etc.)
- Preferred test runner:

## My preferences this session
- (e.g. "prefer TypeScript strict mode", "use Zod for all schema validation", "explain migrations before running them")

## API keys / secrets
- (never commit real values here — reference .env instead)
