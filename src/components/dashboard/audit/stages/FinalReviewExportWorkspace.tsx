// =============================================================================
// FinalReviewExportWorkspace — Stage 8 of 8.
//
// Phase 2 deliverable per HANDOVER. Phase A: shared StagePlaceholder. Phase B
// (Phase 2 of source build) implements final approval + deliverable export
// (docx/markdown).
// =============================================================================

import StagePlaceholder from '../StagePlaceholder';

export default function FinalReviewExportWorkspace() {
  return <StagePlaceholder stage="FINAL_REVIEW_EXPORT" />;
}
